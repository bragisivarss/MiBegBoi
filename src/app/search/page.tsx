"use client";
import { Header, SearchProvider} from "@/components";

const SearchPage: React.FC = () => {
    return (
        <>
            <Header />;
            <SearchProvider />
        </>
    );
};
export default SearchPage;
